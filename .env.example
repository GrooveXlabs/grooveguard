# GrooveGuard environment variables
# Copy to .env and fill in values if needed for development

# No secrets required for scanning — GrooveGuard is read-only.
# Set this only if you are developing custom fetch-based rules.
# GROOVEGUARD_RULES_PATH=./custom-rules.yaml
